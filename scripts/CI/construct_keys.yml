construct_keys:
  stage: .pre
  script:
    # Build keys.json first
    - |
      cat > src/main/resources/keys.json << EOF
      {
        "weather": $weather_api_key,
        "maps": $map_api_key
      }
      EOF